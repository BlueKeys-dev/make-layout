# Implementation Plans

Generated by the improve skill on 2026-09-01 at commit `17cbc50` on branch `codex/reusable-slot-layouts`. Execute in order unless dependencies say otherwise.

## Execution order & status


| Plan | Title                                                       | Priority | Effort | Depends on | Status |
| ---- | ----------------------------------------------------------- | -------- | ------ | ---------- | ------ |
| 001  | Fix layout-library stale error and page-index drift         | P1       | S      | —          | TODO   |
| 002  | Deduplicate container-board creation and layout target rect | P1       | M      | —          | TODO   |
| 003  | Decompose DesignEditor into focused modules                 | P1       | L      | 002        | TODO   |
| 004  | Validate persisted canvas config at load boundary           | P2       | S      | —          | TODO   |
| 005  | Align foreignObject SVG sanitization with main SVG rules    | P2       | S      | —          | TODO   |
| 006  | Expand core WebMCP authoring                               | P1       | M      | —          | DONE   |




## Dependency notes

- **003** should follow **002** so board/layout helpers exist before moving code out of `DesignEditor.tsx`.
- **004** and **005** are independent and can run in parallel with 001–003.



## Findings considered and rejected

- **Export captures only primary logical page bounds**: by-design for PDF/PPTX page export; multi-board infinite canvas is a product gap, not a regression from this branch. Defer a separate “export selection / all boards” plan until product asks for it.
- **Layout template cross-tab race**: real but low frequency; not worth a storage-versioning plan until multi-tab editing is a stated requirement.
- **animationhome.tsx size (1265 lines)**: pre-existing debt, not introduced by this branch’s core slot-layout work; defer to a dedicated AnimationHome decomposition plan.



## Thermo-nuclear review verdict

**Do not approve the branch on maintainability grounds alone.** `DesignEditor.tsx` is ~1942 lines and this diff adds orchestration, layout-library handlers, and three inline container-board constructors without decomposition. Behavior fixes (export, Mermaid labels, canvas defaults) are sound; structure needs follow-up per plans above.
